# AFFIX: ZERO 신규 개발 규칙

최신 사용자 결정은 기존 코드 전체 폐기와 타 엔진 새 시작이다. 현재 개발선은 restart/unity-6, Unity 6.3 LTS 6000.3.24f1 / C# / 2D Built-in이다. 이전 Godot 코드, E0/V0, assets, 과거 렌더 설계는 새 트리에서 전부 삭제했다. Git 역사에서 코드나 거절된 아트를 복원하지 않는다. 플레이 방향과 콘텐츠 아이디어만 docs/PRODUCT_BRIEF.md로 이어간다.

먼저 docs/HANDOFF.md와 docs/RESTART_DECISION.md를 읽는다. 오래된 PR #18과 Godot 폴더에서 작업하지 않는다. main은 승인 없이 병합하지 않는다. 사용자 로컬 폴더, 세이브, stash, Git 역사를 삭제하거나 force-push하지 않는다. 사용자는 ChatGPT 채팅+GitHub로 개발하고 Codex 실행을 원하지 않는다.

검증된 외부 2D 리소스의 라이선스/공개 재배포/실제 동작을 확인한다. 기존 어두운 폐허/붉은 균열/다크 UI, 임의 막대형 대체 그림을 금지한다. 단일 원화 움직임을 프레임 애니메이션이라고 하지 않는다. 아트 검수 없이 스폰 수/가챠/환생/성능 하네스부터 늘리지 않는다.

현재 U0는 기반 소스다. 코어 C# 검사와 Unity 컴파일/에디터 실행/플레이/시각 승인은 각각 구분한다. 실행하지 않은 결과를 PASS라고 쓰지 않는다. Assets의 .meta를 커밋하고 캐시만 무시한다. Unity 설치/계정 활성화는 사용자의 실제 로컬 작업이다. 정확한 설치 버전과 필요한 동작을 안내한다.

트러블슈팅은 문제 발생 지점 | 원인 분석 | 해결 방법 및 적용된 코드 개념 | 배운 점 표에 기록한다. 모든 답변 끝에는 진행 상황 체크포인트의 구현/이번 변경/다음 작업 3항목을 쓴다. 채팅 밖 작업 약속은 하지 않는다.

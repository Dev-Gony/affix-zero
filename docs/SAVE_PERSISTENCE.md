# 로컬 성장 저장

2026-09-26 저장 구현 작업. 실제 실행·복원 검증을 완료했으며 아래 근거를 기록한다.

Windows 플레이어는 Unity `Application.persistentDataPath` 아래 `profile-v1.json` 한 슬롯을 사용한다. 장착 무기·어픽스·강화, 24칸 가방과 미수거 전리품, 특성 투자/잔여 포인트, 누적 XP·가용 골드, 이미 지급한 처치와 아이템 ID를 함께 저장한다. 새 실행에서 던전 위치·적·체력·공격 시간표는 초기화하고 입구에서 사냥 시작을 기다린다. 성장 상태를 복원하는 저장이며 중간 전투 프레임이나 오프라인 보상을 복원하지 않는다.

처치 보상, 수거, 장착, 버리기, 특성 투자/초기화, 강화, 클리어 전리품 생성 직후 동기 저장한다. 종료와 앱 pause에도 저장한다. 저장 실패 시 현재 메모리 상태와 기존 파일을 유지하며 사냥을 중단한다. HUD에 실패와 저장 재시도를 표시한다. 읽기 실패로 임시 표시한 새 프로필을 기존 파일 위에 저장하지 않는다.

DTO는 순수 C#이고 Unity JsonUtility는 표시 계층의 어댑터에서만 사용한다. 스키마1·SHA256·능력치/특성 전제·가방 크기·소유 ID·보상 이력 일관성을 검증한다. 체크섬은 우발적 손상 검출이며 치트 방지 보장이 아니다. 새 리소스·보상 규칙을 추가할 때 스키마 호환성을 함께 검토한다.

저장은 같은 폴더의 임시 파일을 디스크까지 flush한 뒤 원자 교체한다. 이전 정상 파일은 `.bak`으로 남긴다. 정상 파일이 손상됐고 백업이 유효하면 백업을 복원하며, 이후 저장할 때 손상 원본을 별도 이름으로 보존한다. 미완료 `.tmp`도 별도 이름으로 보존한다. 지원하지 않는 버전·접근 오류·둘 다 손상된 경우 자동 초기화하지 않는다. `.lock`을 세션 동안 독점 열어 두 실행본의 동시 쓰기를 막고, 로드 뒤 외부에서 바뀐 파일도 임의로 덮어쓰지 않는다. 파일 크기는 2MiB로 제한하며 초과 시 실패를 표시한다. 긴 기간의 이력 축약은 후속 범위다.

기존 `-affixAutoHuntTest`·`-affixAutoHuntSafetyTest`·`-affixSmokeTest`와 Editor 검증은 임시 메모리 프로필로 격리해 사용자 저장을 읽거나 쓰지 않는다. 저장 검사는 `-affixSaveTest -affixSaveMode write|read -affixSaveDir <절대 시험 폴더> -affixReportDir <절대 보고 폴더>`를 사용하는 별도 실제 Windows 프로세스다. 생산 저장 경로를 시험 폴더로 지정하면 거절한다. 파일을 고장 내는 검사는 새 시험 폴더 안에서만 수행하며 사용자 저장은 손대지 않는다.

저장 추가는 MVP의 한 단계다. 전체 MVP 완료에는 저장 재실행·복구 검사뿐 아니라 장시간 사냥, 안전 중단 후 재개, 실제 조작 검수가 필요하다.

## 실행 결과

- Core202 checks PASS. Unity JsonUtility로 새 프로필·미수거 있음·수거 후 장착/강화 상태와 미지원 스키마4검사 PASS. .NET 직렬화와 구분한다.
- Windows 빌드2026-09-26T07:00:29.9473535Z, GUID baf9e73795d3447e9adc1d13daabe1d2, 오류0/경고0.
- write run0609829f35f74ee5bb7c6dd5180c4498: 실제6처치, 첫 검 장착·분노2/정밀1·강화+1, 공격52·XP150·골드40·잔여3포인트·가방1·미수거 클리어 검. 약23.8초.
- read runa7e98c28ae5940278eba0789938e976c: 새 프로세스에서 직렬화 상태 전체 일치, HUD/실제 공격52, 입구 정지 확인. 재개 후 미수거 자동 회수, 새2처치로XP200·골드56·잔여5포인트·가방2. 이전 처치 토큰 거절. 약5.04초.
- 실제 Windows 백업 복구 후 재개 PASS. 손상 원본 byte hash 동일 보존. 양쪽 손상과 미래 schema2는 load-block 기대대로 종료하고 primary/backup byte hash를 유지했다. 이 두 probe의 FAIL은 정상 플레이 성공이 아니라 의도한 차단이며 외부 기대결과는 PASS다.
- 최신1080p 자동사냥 회귀: 임시프로필3순환18처치4수거/사망0, XP450·골드144. 이 검사는 파일 저장 부하 검사가 아니다. 저장이 활성인 실행은 위write/read다.

보고서는 `validation/save/`. OS 물리 입력 검사·사용자 시각 승인·강제 OS종료/전원차단 실험은 하지 않았다. Core 파일 검사는 교체 실패/임시파일 보존을 검사한다. 저장 덕분에 종료 후 성장 유지는 가능하지만 전체 MVP는 아직 아니다.
